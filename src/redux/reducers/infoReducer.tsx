const initialState = {
    name: String,
    email: String,
    image: String,
    phone: String,
    facebook_id: String,
    role: Number,
    role_admin: Number,
}

export default function actionForReducer(state = initialState, payload:any) {

}